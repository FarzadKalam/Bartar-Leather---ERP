# 🔧 راهنمای سریع رفع مشکل

## مشکل: فرم محصول ذخیره نمی‌شود و دسته‌بندی خالی است

### ✅ گام 1: بررسی Console مرورگر
1. صفحه را باز کنید (F5)
2. Developer Console را باز کنید (F12)
3. به تب **Console** بروید
4. دنبال این پیام‌ها باشید:

```
🚀 Running in development mode
💡 Testing database connection...
🔍 Testing dynamic_options table...
```

### 📋 حالت 1: اگر خطای "Table does not exist" دیدید

**یعنی جدول `dynamic_options` در دیتابیس شما وجود ندارد.**

**راه حل:**
1. به پنل **Supabase** بروید
2. از منوی سمت چپ **SQL Editor** را انتخاب کنید
3. فایل `setup-database.sql` را باز کنید
4. تمام محتوای آن را کپی کنید
5. در SQL Editor پیست کنید
6. دکمه **RUN** را بزنید
7. صفحه را Refresh کنید (F5)

### 📋 حالت 2: اگر پیام "No product_categories found" دیدید

**یعنی جدول وجود دارد اما داده ندارد.**

**راه حل:**
فقط بخش INSERT های فایل `setup-database.sql` را اجرا کنید (از خط 15 به بعد)

### 📋 حالت 3: اگر جدولی با داده‌ها دیدید (✅)

**یعنی دیتابیس درست است. مشکل از جای دیگر است.**

در Console این دستور را اجرا کنید:
```javascript
testDynamicOptions()
```

اگر داده‌ها را دیدید، مشکل از frontend است. لطفاً:
1. وقتی فرم را باز می‌کنید، Console را بررسی کنید
2. وقتی دکمه ذخیره را می‌زنید، Console را بررسی کنید
3. اسکرین‌شات از خطاها بگیرید

---

## 🐛 اخطارهای برطرف شده:

- ✅ Upload component: از `fileList` به جای `value` استفاده می‌کند
- ✅ Table rowKey: دیگر از `index` استفاده نمی‌کند
- ⚠️ useForm warning: معمولاً مشکلی ایجاد نمی‌کند

---

## 📞 اگر هنوز مشکل دارید:

لطفاً این اطلاعات را برایم بفرستید:
1. اسکرین‌شات از Console (F12 > Console tab)
2. اسکرین‌شات از Network tab هنگام Submit فرم
3. خروجی دستور `testDynamicOptions()` در Console

---

## 🎯 تست سریع:

در Console مرورگر این دستور را بزنید:
```javascript
testDynamicOptions()
```

باید خروجی شبیه این ببینید:
```
✅ Table exists and accessible
✅ Found 6 product categories:
📦 Available categories: ['product_categories', 'leather_color', ...]
```
