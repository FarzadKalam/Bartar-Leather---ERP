# 📊 SELECT/MULTI_SELECT فلو ڈایاگرام

## 1️⃣ ModuleList (جدول)

```
┌──────────────────────────────────────────────────────────────┐
│                        ModuleList_Refine                      │
├──────────────────────────────────────────────────────────────┤
│  useEffect(() => {                                            │
│    fetchOptions(); ← Database سے dynamic_options لاتا ہے   │
│  }, [moduleConfig]);                                          │
│                                                               │
│  const [dynamicOptions] = useState({                          │
│    'leather_color': [                                         │
│      { label: 'سیاہ', value: 'black' },                      │
│      { label: 'قہوائی', value: 'brown' },                    │
│      { label: 'سرخ', value: 'red' }                          │
│    ]                                                          │
│  });                                                          │
└──────────────────────────────────────────────────────────────┘
                             ↓
┌──────────────────────────────────────────────────────────────┐
│                  SmartTableRenderer                           │
├──────────────────────────────────────────────────────────────┤
│  const columns = tableFields.map(field => {                  │
│    // MULTI_SELECT ستون                                     │
│    filters: field.options || dynamicOptions[category]        │
│    onFilter: (value, record) => {                            │
│      return record[field.key].includes(value);              │
│    }                                                          │
│    render: (value) => {                                      │
│      return value.map(v => (                                 │
│        <Tag color="cyan">{getLabel(v)}</Tag>                │
│      ))                                                       │
│    }                                                          │
│  })                                                           │
└──────────────────────────────────────────────────────────────┘
                             ↓
┌──────────────────────────────────────────────────────────────┐
│                      صارف کے پاس                              │
├──────────────────────────────────────────────────────────────┤
│  leather_colors ▼  ← Filter icon موجود                      │
│  ┌────────────────┐                                           │
│  │ ☐ سیاہ        │  ← منتخب کریں                            │
│  │ ☐ قہوائی      │                                           │
│  │ ☐ سرخ         │                                           │
│  └────────────────┘                                           │
│                                                               │
│  [سیاہ] [قہوائی] [سرخ]  ← Cyan tags نمایش                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 2️⃣ ModuleShow (جزئیات)

```
┌──────────────────────────────────────────────────────────────┐
│                        ModuleShow                             │
├──────────────────────────────────────────────────────────────┤
│  const fetchOptions = async () => {                          │
│    // Supabase سے dynamic_options لائیں                      │
│    const { data } = await supabase                           │
│      .from('dynamic_options')                                │
│      .select('label, value')                                 │
│      .eq('category', 'leather_color');                       │
│                                                               │
│    setDynamicOptions({ leather_color: data });              │
│  }                                                            │
│                                                               │
│  const renderField = (field, value) => {                     │
│    if (field.type === FieldType.MULTI_SELECT) {              │
│      return (                                                │
│        <div>                                                 │
│          {value.map(v => (                                  │
│            <Tag color="cyan">                               │
│              {getSingleOptionLabel(field, v)}               │
│            </Tag>                                            │
│          ))}                                                 │
│        </div>                                                │
│      );                                                      │
│    }                                                         │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘
                             ↓
┌──────────────────────────────────────────────────────────────┐
│                      صارف کے پاس                              │
├──────────────────────────────────────────────────────────────┤
│  رنگ های چرم (چند انتخابی)                                  │
│  ┌──────────────────────────────────────┐                   │
│  │  [سیاہ] [قہوائی] [سرخ]                │  ← Cyan tags   │
│  └──────────────────────────────────────┘                   │
│                                                               │
│  Edit دبائیں ↓                                              │
│  ┌──────────────────────────────────────┐                   │
│  │  [سیاہ X] [قہوائی X] [سرخ X] + Add   │                 │
│  │                                      │                   │
│  │  Dropdown:                           │                   │
│  │  ☐ نیلا                              │                   │
│  │  ☐ سبز                               │                   │
│  └──────────────────────────────────────┘                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 3️⃣ EditableTable (BOM جدول)

```
┌──────────────────────────────────────────────────────────────┐
│                      EditableTable                            │
├──────────────────────────────────────────────────────────────┤
│  const renderCell = (col, value, index) => {                 │
│                                                               │
│    // Display Mode                                           │
│    if (!isEditing) {                                         │
│      if (col.type === FieldType.MULTI_SELECT) {              │
│        const labels = value.map(v => {                      │
│          return find_label(v);                              │
│        }).join(', ');                                        │
│        return <span>{labels}</span>;  // سیاہ، قہوائی، سرخ  │
│      }                                                       │
│    }                                                         │
│                                                               │
│    // Edit Mode                                              │
│    if (isEditing) {                                          │
│      if (col.type === FieldType.MULTI_SELECT) {              │
│        return (                                              │
│          <Select                                             │
│            mode="multiple"                                   │
│            value={value}                                     │
│            options={dynamicOptions[category].map(o => ({   │
│              label: o.label,  // فارسی                      │
│              value: o.value   // انگریزی                    │
│            }))}                                              │
│          />                                                  │
│        );                                                    │
│      }                                                       │
│    }                                                         │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘
                             ↓
┌──────────────────────────────────────────────────────────────┐
│                        جدول نمایش                             │
├──────────────────────────────────────────────────────────────┤
│  │ نام       │ رنگ های چرم │ استعمال │ کل          │       │
│  ├─────────┼────────────┼────────┼─────────┤       │
│  │ Premium │ سیاہ،      │ 10     │ 5000    │       │
│  │ Leather │ قہوائی،    │        │         │       │
│  │         │ سرخ        │        │         │       │
│  │         │                    │         │       │
│  │         │ [Edit] →  Multi    │         │       │
│  │         │          select    │         │       │
│  │         │          dropdown  │         │       │
│  └─────────┴────────────┴────────┴─────────┘       │
└──────────────────────────────────────────────────────────────┘
```

---

## 4️⃣ Data Flow

```
┌─────────────────────────────────────────────────────────────┐
│ Database: products                                          │
├─────────────────────────────────────────────────────────────┤
│ id  │ name         │ leather_colors                          │
│─────┼──────────────┼─────────────────────────────────────── │
│ 1   │ Premium      │ ['black', 'brown', 'red']              │
│ 2   │ Standard     │ ['blue']                               │
│ 3   │ Budget       │ NULL                                   │
└─────────────────────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────────────────────┐
│ Database: dynamic_options                                   │
├─────────────────────────────────────────────────────────────┤
│ id │ category        │ value  │ label   │ is_active         │
│────┼─────────────────┼────────┼─────────┼───────────────── │
│ 1  │ leather_color   │ black  │ سیاہ    │ true             │
│ 2  │ leather_color   │ brown  │ قہوائی  │ true             │
│ 3  │ leather_color   │ red    │ سرخ     │ true             │
│ 4  │ leather_color   │ blue   │ نیلا    │ true             │
└─────────────────────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────────────────────┐
│ ModuleList_Refine.fetchOptions()                            │
├─────────────────────────────────────────────────────────────┤
│ setDynamicOptions({                                         │
│   'leather_color': [                                        │
│     { label: 'سیاہ', value: 'black' },                     │
│     { label: 'قہوائی', value: 'brown' },                   │
│     { label: 'سرخ', value: 'red' },                        │
│     { label: 'نیلا', value: 'blue' }                       │
│   ]                                                         │
│ })                                                          │
└─────────────────────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────────────────────┐
│ SmartTableRenderer.render()                                 │
├─────────────────────────────────────────────────────────────┤
│ columns.map(col => {                                        │
│   if (col.key === 'leather_colors') {                      │
│     return {                                                │
│       filters: [                                            │
│         { text: 'سیاہ', value: 'black' },                  │
│         { text: 'قہوائی', value: 'brown' },                │
│         // ...                                              │
│       ],                                                    │
│       render: (value) => [                                 │
│         <Tag>سیاہ</Tag>,                                   │
│         <Tag>قہوائی</Tag>,                                 │
│         <Tag>سرخ</Tag>                                      │
│       ]                                                     │
│     }                                                       │
│   }                                                         │
│ })                                                          │
└─────────────────────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────────────────────┐
│ صارف کے پاس نمایش:                                          │
├─────────────────────────────────────────────────────────────┤
│  leather_colors ▼                                           │
│  Premium: [سیاہ] [قہوائی] [سرخ]                            │
│  Standard: [نیلا]                                           │
│  Budget: -                                                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 5️⃣ Filtering Flow

```
صارف Filter دباتا ہے
        ↓
┌─────────────────────────────────────┐
│ Filter Dropdown                     │
│ ☐ سیاہ      ← label (فارسی)        │
│ ☐ قہوائی                           │
│ ☐ سرخ                              │
└─────────────────────────────────────┘
        ↓
صارف 'سیاہ' منتخب کرتا ہے
        ↓
┌─────────────────────────────────────┐
│ SmartTableRenderer.onFilter()        │
│                                     │
│ if (field.type === MULTI_SELECT) {  │
│   const recordValue =               │
│     ['black', 'brown', 'red'];      │
│                                     │
│   return recordValue.includes(      │
│     'black'  ← منتخب value          │
│   );                                │
│ }                                   │
│ → true ✅                           │
└─────────────────────────────────────┘
        ↓
صرف 'black' والی records دیکھائی دیں
```

---

## 6️⃣ Component Hierarchy

```
App
└── ModuleList_Refine
    ├── useEffect → fetchOptions()
    │   └── setDynamicOptions()
    │   └── setRelationOptions()
    │
    └── SmartTableRenderer
        ├── props.dynamicOptions ✅
        ├── props.relationOptions ✅
        │
        └── columns.map(field => {
            ├── filters: dynamicOptions[category]
            │
            └── render: getSingleOptionLabel()
                        getSingleOptionLabel()
                        getSingleOptionLabel()
        })

App
└── ModuleShow
    ├── useEffect → fetchOptions()
    │   └── setDynamicOptions()
    │   └── setRelationOptions()
    │
    └── renderField(field, value) {
        if (MULTI_SELECT) {
          return value.map(v =>
            <Tag>{getSingleOptionLabel(...)}</Tag>
          )
        }
    }

App
└── EditableTable
    ├── props.dynamicOptions (پہلے سے)
    │
    └── renderCell(col, value) {
        if (MULTI_SELECT) {
          Display: labels.join(', ')
          Edit: <Select mode="multiple" />
        }
    }
```

---

## 🎨 Visual Summary

```
      DATABASE               STATE                 COMPONENT              USER
    ┌─────────┐          ┌──────────┐          ┌─────────────┐      ┌─────────┐
    │ Products│          │Dynamic   │          │SmartTable   │      │ Browser │
    │ leather │          │Options   │          │Renderer     │      │         │
    │_colors: │          │          │          │             │      │         │
    │['black' │──fetch───│leather_  │─props───│ Render:     │─────▶│ [سیاہ]  │
    │'brown'] │          │color: [  │         │getSingle... │      │ [قہوائی]│
    │         │          │{label:   │         │             │      │ [سرخ]   │
    │         │          │'سیاہ',   │         │ Filters:    │      │         │
    │         │          │value:    │         │ سیاہ,       │      │ Filter: │
    │         │          │'black'}  │         │ قہوائی,     │      │ ▼       │
    │         │          │]         │         │ سرخ         │      │ سیاہ✓   │
    └─────────┘          └──────────┘         └─────────────┘      │ قہوائی  │
                                                                     │ سرخ     │
                                                                     └─────────┘
```

---

**خلاصہ:** تمام فیلدز فارسی میں ہیں، filtering کام کرتی ہے، MULTI_SELECT tags میں نمایش ہے! ✅
